

install: make #builds the fptree algorithm the current repo and run the tests.

notifications:
  irc: "chat.freenode.net#ublab"
  
  template:
      - "%{repository}#%{build_number} (%{branch} - %{commit} : %{author}): %{message}"
      - "Build details : %{build_url}"
